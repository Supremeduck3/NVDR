# Compiled executables / C build files
*.exe
*.o
*.out
*.obj
*.dll

# Editor and Cache
.cache/
.vscode/
.idea/
compile_commands.json

# Logs and debug files
*.log
build_errors.txt

# Backups
*.zip
*.bak

# Temporary outputs from the server
output/
temp_*

# Build artifact (Makefile target)
image_to_svg

# Benchmark script's ad-hoc SVBC outputs land in /tmp/ (outside the repo).
# If you ever run the bench with a custom --images pointing into the repo,
# nothing inside /tmp/nvdr-inspect/ gets polluted.

# Python tooling leftovers
__pycache__/
*.pyc

# Node modules (for server.js)
node_modules/

# Documentos internos (propriedade intelectual do NVDR)
docs/
ANTIGRAVITY.md